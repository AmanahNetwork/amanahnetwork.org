{
  "buildCommand": "cd amanah-frontend && npm install && npm run build",
  "outputDirectory": "amanah-frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/Amanah-backend/api/server.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
